version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - RESUME_PATH=/app/Hassan_Khan_resume.docx
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    restart: always
